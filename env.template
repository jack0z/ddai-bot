# MongoDB Configuration for Point Tracking
# Get your MongoDB URI from MongoDB Atlas or your self-hosted MongoDB instance
# Example: mongodb+srv://username:password@cluster.mongodb.net/database_name
MONGODB_URI=your_mongodb_connection_string_here

# Optional: Set to 'production' for production environment
NODE_ENV=development

# Optional: Set custom database name (default: ddai_farming)
DB_NAME=ddai_farming

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual MongoDB connection details
# 3. Save the file - it will be automatically loaded by the bot
# 4. The .env file should NOT be committed to git (it's in .gitignore) 